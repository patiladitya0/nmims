import React from 'react';
import './setting.css';

const Settings = () => {
    return (
        <div className="settings-container">
            Critical info
        </div>
    );
};

export default Settings;
